# `pat-nvi`

Documents the *non-virtual interface* design pattern in Rust, which allows for a very clear separation of the *outside* of the abstraction to which clients / external world can connect to (`Database`), and the guts of it (`nvi_impl` module).
